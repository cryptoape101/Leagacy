import React from 'react';
import { Button } from '@mui/material';

// Simple Button wrapper - now using Material-UI defaults
const FantasyButton = (props) => {
  return <Button {...props} />;
};

export default FantasyButton;